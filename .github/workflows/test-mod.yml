name: WAF/ModSecurity Tests

on:
  push:
    branches: [ main, auth ]
    paths:
      - 'app/nginx/**'
      - 'docker-compose.yml'
      - '.github/workflows/test-mod.yml'
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test-waf:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🐳 Build and start services
        run: |
          docker compose up -d --build
          sleep 20

      - name: ⏳ Wait for services to be ready
        run: |
          echo "Waiting for PostgreSQL..."
          sleep 10

      - name: 🔍 Check ModSecurity is loaded
        run: |
          echo "Checking if ModSecurity module is loaded..."
          docker exec nginx nginx -V 2>&1 | grep modsecurity
          echo "✅ ModSecurity module found!"
          
          echo "Checking loaded rules..."
          docker logs nginx 2>&1 | grep "ModSecurity-nginx" || echo "⚠️ No ModSecurity logs yet"

      - name: 🛡️ Test SQL Injection Protection
        run: |
          echo "Testing SQL Injection block..."
          
          response=$(curl -k -s -o /dev/null -w "%{http_code}" -X POST \
            https://localhost:8443/api/auth/login \
            -H "Content-Type: application/json" \
            -d '{"email":"admin@test.com OR 1=1-- ","password":"test"}')
          
          echo "Response code: $response"
          
          if [ "$response" == "403" ] || [ "$response" == "401" ]; then
            echo "✅ SQL Injection blocked or rejected (code: $response)"
          else
            echo "⚠️ Got response: $response (expected 403 or 401)"
          fi

      - name: 🛡️ Test XSS Protection
        run: |
          echo "Testing XSS block..."
          
          response=$(curl -k -s -o /dev/null -w "%{http_code}" -X POST \
            https://localhost:8443/api/auth/register \
            -H "Content-Type: application/json" \
            -d '{"email":"test@test.com","password":"pass123","name":"<script>alert(1)</script>"}')
          
          echo "Response code: $response"
          
          if [ "$response" == "403" ]; then
            echo "✅ XSS blocked (403)"
          else
            echo "⚠️ Got response: $response (expected 403)"
          fi

      - name: 🛡️ Test Rate Limiting
        run: |
          echo "Testing rate limiting on login endpoint..."
          
          for i in {1..7}; do
            code=$(curl -k -s -o /dev/null -w "%{http_code}" -X POST \
              https://localhost:8443/api/auth/login \
              -H "Content-Type: application/json" \
              -d '{"email":"test@test.com","password":"wrong"}')
            
            echo "Request $i: HTTP $code"
            
            if [ $i -gt 5 ] && [ "$code" == "503" ]; then
              echo "✅ Rate limit working!"
              break
            fi
            
            sleep 0.5
          done

      - name: ✅ Test Valid Registration
        run: |
          echo "Testing legitimate request..."
          
          unique_email="user$(date +%s)@test.com"
          
          response=$(curl -k -s -w "\n%{http_code}" -X POST \
            https://localhost:8443/api/auth/register \
            -H "Content-Type: application/json" \
            -d "{\"email\":\"$unique_email\",\"password\":\"SecurePass123\",\"name\":\"John Doe\"}")
          
          http_code=$(echo "$response" | tail -n1)
          
          echo "Response code: $http_code"
          
          if [ "$http_code" == "201" ]; then
            echo "✅ Valid request allowed (201)"
          else
            echo "⚠️ Got response: $http_code"
          fi

      - name: 🔍 Check Security Headers
        run: |
          echo "Testing security headers..."
          
          headers=$(curl -k -s -I https://localhost:8443/)
          
          echo "$headers"
          echo ""
          
          echo "$headers" | grep -i "X-Frame-Options" && echo "✅ X-Frame-Options present" || echo "❌ Missing"
          echo "$headers" | grep -i "X-Content-Type-Options" && echo "✅ X-Content-Type-Options present" || echo "❌ Missing"
          echo "$headers" | grep -i "X-XSS-Protection" && echo "✅ X-XSS-Protection present" || echo "❌ Missing"

      - name: 📋 Show ModSecurity audit log
        if: always()
        run: |
          echo "📋 ModSecurity audit log:"
          docker exec nginx cat /var/log/modsec_audit.log 2>/dev/null || echo "No audit log found (no attacks detected yet)"

      - name: 📋 Show service logs
        if: always()
        run: |
          echo "=== NGINX Logs ==="
          docker logs nginx --tail=30
          echo ""
          echo "=== Auth Service Logs ==="
          docker logs auth_service --tail=20

      - name: 🧹 Cleanup
        if: always()
        run: |
          docker compose down -v
          docker system prune -f