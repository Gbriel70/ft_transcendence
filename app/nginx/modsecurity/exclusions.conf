# ===== EXCLUSÕES (FALSE POSITIVES) =====

# Permitir JSON válido no body
SecRuleRemoveById 920273

# Ajustar detecção de XSS para permitir HTML válido em contextos específicos
SecRuleUpdateTargetById 941100 "!REQUEST_COOKIES"
SecRuleUpdateTargetById 941110 "!REQUEST_COOKIES"

# Desabilitar regra específica para endpoint /api/auth/register
SecRule REQUEST_URI "@streq /api/auth/register" \
    "id:10100,\
    phase:1,\
    pass,\
    ctl:ruleRemoveById=920273,\
    nolog"